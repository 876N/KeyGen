[workspace]
resolver = "2"
members = [
    "crates/shared",
    "crates/keygen",
    "crates/stub",
]

[profile.release]
opt-level = 3
lto = "fat"
codegen-units = 1
strip = true
panic = "abort"
overflow-checks = false
